spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    # 테이블 생성 후에 data.sql 실행
    defer-datasource-initialization: true
  h2:
    console:
      enabled: true
  security:
    oauth2:
      client:
        registration:
          google:
            client-id: 6636
            client-secret: SEC
            scope:
              - email
              - profile

jwt:
  issuer: amung@gmail.com
  secret_key: SEC

